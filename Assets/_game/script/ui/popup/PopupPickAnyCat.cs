using R3;
using UnityEngine;

public class PopupPickAnyCat : BasePopup
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    protected override void Start()
    {
        base.Start();
    }
}
